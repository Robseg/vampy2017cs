import random
names = ["Geoff","Jeff","Jeph","Jehf","Jaeyph","Geaoph"]

Q = []
for name in random.sample(names, 3):
	Q.append(name)
	print("{0} is joining!".format(joining))

time = 1
while len(Q) > 0:
	time.sleep(6)
	print("It is {0} o' clock and there are {1} people at the diner!".format(time. len(Q)))
	if len(Q) < 30 and random.uniform(1,5) < 0.33
		joining = random.choice(names)
		Q.append(joining)
		print("{0} is joining!".format(joining))
	elif len(Q) > 0:
		leaving = Q.pop(0)
		print("{0} is leaving!".format(leaving))
	hour += 1


print("Closing up shop!")
